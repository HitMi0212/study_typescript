// JS 문자열 선언
// var str = 'hello';

// TS 문자열 선언
let str: string = 'hello';
// 숫자
let num: number = 0;
// 배열
let arr: Array<number> = [1,2,3];
let heroes: Array<string> = ['Cpat', 'Thor', 'Hulk'];
let items: number[] = [1,2,3];

// 튜플
// - 배열 요소의 타입까지 결정
let address: [string, number] = ['gangnam', 100];

// 객체
let obj: object = {};
// 속성 타입까지 결정
let person: {name: string, age: number} = {
    name: 'choi',
    age: 100
};

// 진위값
let show: boolean = true;
